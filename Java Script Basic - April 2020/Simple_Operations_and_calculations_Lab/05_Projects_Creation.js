function projectCreation(fname, pamount) {
    let result = `The architect ${fname} will need ${pamount * 3} hours to complete ${pamount} project/s.`
    console.log(result)
}

projectCreation('Georgi', 9)